/**
 * the question code print will start the save document format of code it genearlly used to open printer 
 * var arr = [2, 56, 78, 34, 65];
var new_arr = arr.map(function(num) {return num / 2;});
print(new_arr);
*/
var arr = [2, 56, 78, 34, 65];var new_arr = arr.map(function(num) {return num / 2;});console.log(new_arr);
/**
 * now the output of this code is 
 * [1,28,39,17,32.5]
 */